# 🚀 신약 개발 연구 어시스턴트 완전한 설정 가이드

## 📋 목차
1. [앱 접속 방법](#1-앱-접속-방법)
2. [AWS Bedrock 연결 설정](#2-aws-bedrock-연결-설정)
3. [웹 검색 설정 (선택사항)](#3-웹-검색-설정-선택사항)
4. [문제 해결](#4-문제-해결)

---

## 1. 앱 접속 방법

### ✅ 현재 실행 상태 확인
터미널에서 다음 명령어로 앱이 실행 중인지 확인:
```bash
cd 25_drug_discovery_agent
source .venv/bin/activate
streamlit run application/app.py
```

### 🌐 접속 주소
앱이 실행되면 다음 주소로 접속:

**로컬 접속 (권장):**
```
http://localhost:8501
```

**네트워크 접속:**
```
http://172.16.0.67:8501
```

### 🔧 접속이 안 될 때
1. **터미널 확인**: "You can now view your Streamlit app" 메시지가 보이는지 확인
2. **포트 충돌**: 8501 포트가 이미 사용 중이면 다른 포트로 실행
   ```bash
   streamlit run application/app.py --server.port 8502
   ```
3. **방화벽**: 방화벽이 8501 포트를 차단하고 있는지 확인
4. **브라우저 캐시**: 브라우저 캐시를 지우고 다시 시도

---

## 2. AWS Bedrock 연결 설정

### 📌 필수 단계

#### Step 1: AWS CLI 설치 확인
```bash
aws --version
```
설치되어 있지 않다면:
- macOS: `brew install awscli`
- 기타: https://aws.amazon.com/cli/

#### Step 2: AWS 자격 증명 설정

**방법 1: AWS Configure 사용 (권장)**
```bash
aws configure --profile drug-discovery
```

입력 정보:
```
AWS Access Key ID: [IAM 사용자의 Access Key]
AWS Secret Access Key: [IAM 사용자의 Secret Key]
Default region name: us-west-2
Default output format: json
```

**방법 2: 환경 변수 설정**
```bash
export AWS_ACCESS_KEY_ID="your-access-key"
export AWS_SECRET_ACCESS_KEY="your-secret-key"
export AWS_DEFAULT_REGION="us-west-2"
```

**방법 3: 자격 증명 파일 직접 편집**
```bash
nano ~/.aws/credentials
```

다음 내용 추가:
```ini
[drug-discovery]
aws_access_key_id = YOUR_ACCESS_KEY
aws_secret_access_key = YOUR_SECRET_KEY
```

그리고 config 파일 설정:
```bash
nano ~/.aws/config
```

```ini
[profile drug-discovery]
region = us-west-2
output = json
```

#### Step 3: 프로파일 활성화
```bash
export AWS_PROFILE=drug-discovery
```

매번 설정하기 귀찮다면 `.zshrc` 또는 `.bashrc`에 추가:
```bash
echo 'export AWS_PROFILE=drug-discovery' >> ~/.zshrc
source ~/.zshrc
```

#### Step 4: 연결 테스트
```bash
aws bedrock list-foundation-models --region us-west-2
```

성공하면 사용 가능한 모델 목록이 출력됩니다.

### 🔑 IAM 권한 요청

관리자에게 다음 권한을 요청하세요:

**필수 권한 (JSON 정책):**
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "bedrock:InvokeModel",
                "bedrock:InvokeModelWithResponseStream"
            ],
            "Resource": [
                "arn:aws:bedrock:us-west-2::foundation-model/us.anthropic.claude-3-5-sonnet-20241022-v2:0",
                "arn:aws:bedrock:us-west-2::foundation-model/us.anthropic.claude-3-5-haiku-20241022-v1:0",
                "arn:aws:bedrock:us-west-2::foundation-model/us.anthropic.claude-3-7-sonnet-20250219-v1:0",
                "arn:aws:bedrock:us-west-2::foundation-model/us.anthropic.claude-4-sonnet-20250219-v1:0"
            ]
        },
        {
            "Effect": "Allow",
            "Action": [
                "bedrock:GetFoundationModel",
                "bedrock:ListFoundationModels"
            ],
            "Resource": "*"
        }
    ]
}
```

**요청 이유:**
- 신약 개발 연구 어시스턴트 AI 시스템 구축
- 과학 문헌 통합 분석 및 보고서 생성
- 연구 개발 및 학습 목적

### 🎯 Bedrock 모델 액세스 활성화

1. **AWS Console 접속**: https://console.aws.amazon.com/
2. **Bedrock 서비스로 이동**: 검색창에 "Bedrock" 입력
3. **왼쪽 메뉴에서 "Model access" 클릭**
4. **"Manage model access" 버튼 클릭**
5. **다음 모델들 체크:**
   - ✅ Anthropic Claude 4 Sonnet
   - ✅ Anthropic Claude 3.7 Sonnet
   - ✅ Anthropic Claude 3.5 Sonnet
   - ✅ Anthropic Claude 3.5 Haiku
6. **"Request model access" 버튼 클릭**
7. **승인 대기 (보통 즉시 승인됨)**

### ✅ 설정 확인

앱을 실행하고 질문을 입력했을 때:
- ❌ AWS 자격 증명 오류 → Step 2 다시 확인
- ❌ 모델 액세스 오류 → Step 4의 모델 액세스 확인
- ✅ 정상 응답 → 설정 완료!

---

## 3. 웹 검색 설정 (선택사항)

### 🆓 Google Custom Search API (권장)

**장점:**
- 하루 100회 무료
- 전체 웹 검색 가능
- 한국어/영어 지원

**설정 방법:**
1. [Google Cloud Console](https://console.cloud.google.com/) 접속
2. 프로젝트 생성
3. Custom Search API 활성화
4. API 키 생성
5. [Programmable Search Engine](https://programmablesearchengine.google.com/) 에서 검색 엔진 생성
6. `.env` 파일에 추가:
   ```env
   GOOGLE_API_KEY="your-api-key"
   GOOGLE_CSE_ID="your-search-engine-id"
   ```

자세한 내용은 `Google_Search_API_설정가이드.md` 참조

### 💰 Tavily API (고급)

**장점:**
- 월 1000회 무료
- AI 최적화된 검색
- 구조화된 결과

**설정 방법:**
1. [Tavily.com](https://tavily.com) 가입
2. API 키 발급
3. `.env` 파일에 추가:
   ```env
   TAVILY_API_KEY="your-tavily-api-key"
   ```

---

## 4. 문제 해결

### 🔴 앱이 실행되지 않을 때

**증상:** `streamlit run` 명령어 실행 시 오류
```bash
# 가상환경 활성화 확인
source .venv/bin/activate

# 의존성 재설치
uv pip install -r requirements.txt

# 앱 실행
streamlit run application/app.py
```

### 🔴 AWS 연결 오류

**증상:** "Unable to locate credentials"
```bash
# 자격 증명 확인
aws configure list

# 프로파일 확인
cat ~/.aws/credentials
cat ~/.aws/config

# 환경 변수 확인
echo $AWS_PROFILE
echo $AWS_ACCESS_KEY_ID
```

**해결:**
```bash
# 프로파일 재설정
aws configure --profile drug-discovery

# 프로파일 활성화
export AWS_PROFILE=drug-discovery
```

### 🔴 Bedrock 모델 액세스 오류

**증상:** "You don't have access to the model"

**해결:**
1. AWS Console → Bedrock → Model access 확인
2. 필요한 모델들이 "Access granted" 상태인지 확인
3. 리전이 `us-west-2`인지 확인

### 🔴 포트 충돌

**증상:** "Address already in use"

**해결:**
```bash
# 다른 포트로 실행
streamlit run application/app.py --server.port 8502

# 또는 기존 프로세스 종료
lsof -ti:8501 | xargs kill -9
```

### 🔴 브라우저 접속 안 됨

**체크리스트:**
- [ ] 터미널에 "You can now view your Streamlit app" 메시지가 보이는가?
- [ ] `http://localhost:8501` 주소가 정확한가?
- [ ] 방화벽이 8501 포트를 차단하고 있지 않은가?
- [ ] 다른 브라우저로 시도해봤는가?

**해결:**
```bash
# 앱 재시작
# Ctrl+C로 종료 후
streamlit run application/app.py

# 브라우저 캐시 삭제 후 재접속
```

---

## 📞 추가 도움말

### 로그 확인
```bash
# 앱 실행 로그 확인
cd 25_drug_discovery_agent
tail -f ~/.streamlit/logs/streamlit.log
```

### 테스트 질문
설정이 완료되면 다음 질문으로 테스트:
```
"HER2 억제제에 대한 최근 연구 논문을 찾아주세요"
```

### 현재 시스템 상태
- ✅ arXiv - 과학 논문
- ✅ PubMed - 생의학 문헌
- ✅ Google Scholar - 학술 논문 + 인용정보
- ✅ ChEMBL - 생물활성 분자
- ✅ ClinicalTrials.gov - 임상시험
- ❌ Google Search - 웹 검색 (설정 필요)
- ❌ Tavily - AI 웹 검색 (설정 필요)

---

## 🎉 설정 완료 확인

모든 설정이 완료되면:
1. ✅ 브라우저에서 앱 접속 가능
2. ✅ 사이드바에서 모델 선택 가능
3. ✅ 질문 입력 시 정상 응답
4. ✅ 데이터 소스 상태 확인 가능

**문제가 계속되면:**
- `AWS_IAM_권한요청서.md` 참조
- `Google_Search_API_설정가이드.md` 참조
- 터미널 로그 확인