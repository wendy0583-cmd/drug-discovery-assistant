# ⚡ 빠른 시작 가이드 (5분 완성)

## 1️⃣ 앱 실행 (1분)

```bash
cd 25_drug_discovery_agent
source .venv/bin/activate
streamlit run application/app.py
```

**접속 주소:**
```
http://localhost:8501
```

---

## 2️⃣ AWS Bedrock 연결 (3분)

### A. AWS 자격 증명 설정
```bash
aws configure --profile drug-discovery
```

**입력 정보:**
- AWS Access Key ID: `[관리자에게 받은 키]`
- AWS Secret Access Key: `[관리자에게 받은 시크릿]`
- Default region name: `us-west-2`
- Default output format: `json`

### B. 프로파일 활성화
```bash
export AWS_PROFILE=drug-discovery
```

### C. 테스트
```bash
aws bedrock list-foundation-models --region us-west-2
```

---

## 3️⃣ Bedrock 모델 액세스 (1분)

1. https://console.aws.amazon.com/ 접속
2. "Bedrock" 검색
3. 왼쪽 메뉴 "Model access" 클릭
4. 다음 모델 체크:
   - ✅ Claude 4 Sonnet
   - ✅ Claude 3.7 Sonnet
   - ✅ Claude 3.5 Sonnet
   - ✅ Claude 3.5 Haiku
5. "Request model access" 클릭

---

## ✅ 완료!

브라우저에서 `http://localhost:8501` 접속 후 질문 입력:
```
"HER2 억제제에 대한 최근 연구를 찾아주세요"
```

---

## 🔧 문제 해결

### 앱이 안 열려요
```bash
# 포트 변경해서 실행
streamlit run application/app.py --server.port 8502
# 그리고 http://localhost:8502 접속
```

### AWS 연결 오류
```bash
# 자격 증명 재설정
aws configure --profile drug-discovery
export AWS_PROFILE=drug-discovery
```

### 모델 액세스 오류
- AWS Console → Bedrock → Model access 에서 모델 상태 확인
- "Access granted" 상태여야 함

---

## 📚 자세한 설명

더 자세한 내용은 `완전한_설정_가이드.md` 참조